import { library } from "@fortawesome/fontawesome-svg-core";
import * as regular from "@fortawesome/free-regular-svg-icons";
import * as solid from '@fortawesome/free-solid-svg-icons'
library.add(
    // Крестик закрытия
    solid.faXmark,
    // Корзина
    solid.faTrash,
    // Плюсик
    solid.faPlus,
    // Внешняя ссылка
    solid.faArrowUpRightFromSquare,
    // Стрелка вниз-вверх для раскрытия
    solid.faCaretDown,
    solid.faCaretUp,
    // Стрелка вверх-вниз для раскрытия в квадрате
    regular.faSquareCaretDown,
    regular.faSquareCaretUp,
    // Файл Excel
    solid.faFileExcel,
    // Файл с кодом
    solid.faFileCode,
    // Файл со стрелочкой
    solid.faFileExport,
    // Карандаш в квардрате (редактирование)
    solid.faPenToSquare,
    // Галочка
    solid.faCheck,
    // Шестеренка
    solid.faCog,
    // Гаечный ключ
    solid.faWrench,
    // Обновить
    solid.faArrowsRotate,
    // Запретить
    solid.faBan,
    // Горизонтальные полоски (перемещение)
    solid.faBars,
    // Вернуться назад
    solid.faRotateLeft,
    // Монетки
    solid.faCoins,
    // Сравнение
    solid.faCodeCompare,
    // Слайдеры (настройки)
    solid.faSliders,
    // Коробка
    solid.faBox,
    // Воронка фильтра
    solid.faFilter,
    // Иконка подсказки
    solid.faCircleQuestion,
    // Иконка изменения правил парсинга
    solid.faTriangleExclamation,
    // Дублировать правило парсинга
    solid.faCopy
);
